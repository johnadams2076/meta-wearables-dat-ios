name: iOS Samples CI

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Disabled (Use TestFlight Workflow)
    runs-on: macos-latest
    timeout-minutes: 5

    steps:
      - name: CI disabled notice
        run: echo "Mock/simulator CI is disabled. Use CameraAccess TestFlight Deploy workflow."
